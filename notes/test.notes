Log-generating check:
1) start etcd container
2) start ldap containers
3) start etcd watch container: tee output
  1) in new window ...
    1) . ./.profile
    2) cd ~/home/proj/python/etcd3.1
    3) pipenv shell
      1) cd test
      2) python etcd_watch.py 2>&1 | tee out
4) start elect containers
5) get container ids and ips
6) note which container is leader
7) stop a follower container
8) stop the leader container
9) restart the leader container
10) restart the follower container
